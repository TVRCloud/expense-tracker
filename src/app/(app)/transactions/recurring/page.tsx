import { RecurringSeriesListClient } from "@/features/recurring/components/RecurringSeriesListClient";

export const metadata = { title: "Recurring — Finance OS" };

export default function RecurringSeriesListPage() {
  return <RecurringSeriesListClient />;
}
